import {
  Object_default
} from "./chunk-7M2R3MTJ.js";

// node_modules/ol-ext/particule/Base.js
var ol_particule_Base = class olparticuleBase extends Object_default {
  constructor(options) {
    options = options || {};
    super(options);
    this.setOverlay(options.overlay);
    this.coordinate = options.coordinate || [0, 0];
  }
  /** Set the particule overlay
   * @param {ol.Overlay} overl
   */
  setOverlay(overlay) {
    this._overlay = overlay;
  }
  /** Get the particule overlay
   * @return {ol.Overlay}
   */
  getOverlay() {
    return this._overlay;
  }
  /** Draw the particule
   * @param { CanvasRenderingContext2D } ctx
   */
  draw() {
  }
  /** Update the particule
   * @param {number} dt timelapes since last call
   */
  update() {
  }
  /** Update the particule
   * @param {number} dt timelapes since last call
   */
  getRandomCoord(dt) {
    if (this.getOverlay().randomCoord)
      return this.getOverlay().randomCoord();
    else
      return [dt, 0];
  }
};
var Base_default = ol_particule_Base;

export {
  Base_default
};
//# sourceMappingURL=chunk-MYWZCSP7.js.map
